# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Supabase CLI local state
supabase/.temp

# Electron — sortie d'electron-builder (installeurs Windows .exe, dossiers unpacked)
release

# Dossier scratch local : binaire mpv natif (~112 Mo, via `npm run fetch:mpv`) +
# clips de test générés. Rien ici n'est versionné (récupéré/régénéré à la demande).
vendor/

# Signature APK Android — secrets, NE JAMAIS commiter
*.keystore
*.jks
key.properties

# Réglages LOCAUX Claude Code (peuvent contenir des commandes avec secrets)
.claude/settings.local.json
# Worktrees temporaires des agents Claude Code
.claude/worktrees/
